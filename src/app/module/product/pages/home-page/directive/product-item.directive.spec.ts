import { ProductItemDirective } from './product-item.directive';

describe('ProductItemDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductItemDirective();
    expect(directive).toBeTruthy();
  });
});
