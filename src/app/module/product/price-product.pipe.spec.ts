import { PriceProductPipe } from './price-product.pipe';

describe('PriceProductPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceProductPipe();
    expect(pipe).toBeTruthy();
  });
});
