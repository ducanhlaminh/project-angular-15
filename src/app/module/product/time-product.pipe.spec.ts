import { TimeProductPipe } from './time-product.pipe';

describe('TimeProductPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeProductPipe();
    expect(pipe).toBeTruthy();
  });
});
